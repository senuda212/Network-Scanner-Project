# Example environment variables for Network Scanner
# Set a real DATABASE_URL before running with --db or starting the webapp
# Format: postgresql://user:password@host:port/dbname

DATABASE_URL=postgresql://postgres:password@localhost:5432/network_scanner_db
